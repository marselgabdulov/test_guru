<tr>
  <td><%= question.body %></td>
  <td><%= link_to t("action.show"), admin_question_path(question) %></td>
  <td><%= button_to t("action.delete"), admin_question_path(question), class: "btn btn-danger", method: :delete, data: { confirm: t("confirm") } %></td>
</tr>
