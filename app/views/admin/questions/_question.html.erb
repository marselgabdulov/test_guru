<tr>
  <td><%= question.body %></td>
  <td><%= link_to "Show", admin_question_path(question) %></td>
  <td><%= button_to "Delete", admin_question_path(question), method: :delete, data: { confirm: "Are you sure?" } %></td>
</tr>
