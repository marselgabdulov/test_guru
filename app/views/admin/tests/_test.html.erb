<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t("action.show"), admin_test_path(test) %></td>
  <td><%= link_to t("action.edit"), edit_admin_test_path(test) %></td>
  <td><%= link_to t("action.delete"), admin_test_path(test), method: :delete, data: { confirm: t("confirm") } %></td>
</tr>
